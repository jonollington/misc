# install the packages (tidyverse contains ggplot and dplyr which we discussed in the lecture)
install.packages("tidyverse")

# load in the packages
library(tidyverse)

# load in the data
data <- read_csv("data/pl_f9_sample.csv")

# let's have a look at the data
View(data)
